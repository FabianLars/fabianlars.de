import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        type: 'dark',
        /* primary: {
            main: '#0E0B16',
            contrastText: 'FEFEFE',
        },
        secondary: {
            main: '#4717F6',
            contrastText: 'FEFEFE',
        },
        background: {
            default: '#0E0B16',
        }, */
    },
})

export default theme
